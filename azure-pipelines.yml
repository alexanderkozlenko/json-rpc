steps:
- script: dotnet build --verbosity minimal
- script: dotnet test --no-build --verbosity minimal
- script: dotnet pack --no-build --verbosity minimal
- task: PublishTestResults
  inputs:
    testRunner: VSTest
    testResultsFiles: '**/*.trx'